ビルドは`build`ディレクトリの中でやってください

# How to build

```sh
pwd # /home/[USERNAME]/.../data_structures/build ← buildにいることを確認
```

```sh
cmake .. # Makefileの生成. `CMakeLists.txt`を更新したときはこれを実行
```

ビルドするよ

```sh
cmake --build .
```
